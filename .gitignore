# =====================================================================
#  Каталоги вывода сборки — в git не хранятся.
#  Release/ выкладывается отдельно (готовые файлы релизов по номерам).
# =====================================================================
/Build/
/Release/

# =====================================================================
#  Артефакты компиляции/линковки (в т.ч. внутри сторонних библиотек).
#  Исходники библиотек храним, скомпилированное — нет.
# =====================================================================
*.obj
*.o
*.lib
*.a
*.dll
*.so
*.dylib
*.exe
*.pdb
*.ilk
*.idb
*.exp
*.res
*.sbr
*.bsc

# =====================================================================
#  Служебные файлы Code::Blocks / линкера (генерируются при сборке).
# =====================================================================
*.depend
*.layout
*_build_log.html

# =====================================================================
#  Прочее
# =====================================================================
*.tmp
*.log
Thumbs.db
Desktop.ini
